local _ = require("gettext")
return {
    name = "timerplus",
    fullname = _("Timer+"),
    description = _([[A stopwatch and countdown timer. Fullscreen (portrait/landscape) and background (status bar) modes, with flags, presets and second precision. 计时器与倒计时插件：支持全屏（竖屏/横屏）与后台（状态栏）模式。]]),
}
